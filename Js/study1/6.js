const jobArray =["돚거", "법미"];

//객체도 배열 안에 들어간다.
const member =[
    {name: "ㄱㅇㅇ", age:24},
    {name:"김샷건", age:350}
]


console.log(member);

const memberProfile = members =>{
  if (members.length !== 7){
      return 0
  }
  console.log("모두 참석하였습니다.")
};
