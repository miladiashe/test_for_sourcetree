동영상을 출력하는 태그 video
그다지 중요하지 않다고함.
사용법은 두가지가 있다.
방법1 비디오 태그 안에 소스 태그 따로 쓰기
<video controls="controls">
    <source src="~~" type="~~">
</video>
방법2 소스까지 비디오태그와 같이 쓰기
<video src="~~" type="~~" controls="controls">
</video>

폼태그
중요함
정보값을 입력받는 데에 쓰임.
form 태그로 시작/끝. 전체를 묶어준다.
    action 속성=버튼을 눌렀을때 정보값이 전해지는 주소.(서버사람이 정해준다.)
    method 속성= 속성값 get, post 있음. 정보가 가는 방식을 정한다.
        get 방식=도메인주소 뒤에 값이 붙어서 감. 브라우저에 노출됨.(보안에 취약) 디폴트이다.
        post 방식=도메인주소 뒤에 붙지 않고 전송되는 파일의 헤더에 암호화되어 서버로 감.
input 태그로 빈칸을 만든다.(닫는태그 없음)
    type 속성값
        text 평문
        password 비밀번호(****)로 보여짐.
        file 파일 업로드 가능
        radio = 몇개중 하나를 고르는 것
            여러 개의 radio 중 하나를 고를 때 name 은 같아야 하고 value 를 다르게 한다.
        checkbox = 여러개 체크가능
            name 이 같고 value 가 다른 여러개를 만들면 고른 것들의 value 가 배열로 들어간다.
            checked 속성을 넣어 기본값으로 체크되어있는 것을 만들수있다.
        submit = 제출버튼이 된다.
    name 속성 = 이 input 태그 하나에 붙은 이름. 반드시 작성해야 서버에서 사용가능하다.
    size 속성 = input 의 폭. 글자 갯수 기준.
textarea 태그로 길게 쓸 수 있는 빈칸을 만든다. 닫는 태그가 있다. 기본값을 태그 사이에 넣을 수 있다.
    rows 속성 = 세로줄
    cols 속성 = 가로 길이
select 태그로 여러개지 중 한가지를 선택할 수 있는 드롭박스를 만들 수 있다.
select 안에 option 태그로 선택할 옵션을 만든다.
    multiple = "multiple" 속성을 주면 여러개지를 선택할수있게 만든다.

레이아웃 구성 태그 div span
정말 중요함

블럭 요소와 라인 요소로 나눌 수 있다.
    블럭=하나씩 행을 차지함
        리스트 태그의 li나 p 태그 등이 블럭형식의 태그. div 태그도 이러함.
    라인=옆으로 계속 나열된다.
        span 태그가 이러함.
어떤 태그가 블럭이고 라인인지 알아두는것 중요.
