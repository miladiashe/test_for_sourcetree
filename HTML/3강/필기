리스트태그 ul, li, ol
ul 순서 없이 점으로 나타낸다.
ol 순서가 있다. 1. 2. 3. 이런식
li 리스트 하나하나의 요소를 나타낸다.

리스트 태그 dl dt dd
dl 리스트 전체를 dl로 묶는다.
dt dl 안에 리스트 제목을 dt로 묶어서 쓴다.
dd 리스트 하나하나의 요소를 나타낸다.

표를 나타내는 태그 table
예전에는 웹사이트 전체 레이아웃을 만드는데 썼지만 이젠 사용빈도가 많이 줄었다.
table 로 표가 될 부분 전체를 감싼다.
tr 가로줄 행
td 세로줄 열
border 속성으로 표 둘레의 두께를 결정.

이미지를 출력하는 태그 img

사운드를 출력하는 태그 audio
HTML5에 새롭게 추가되었다. 많이 쓰진 않는다.

a 태그 target 속성의 값
self = 자기 창에 열린다
blank = 새 창에 열린다