리스트태그 ul, li, ol
ul 순서 없이 점으로 나타낸다.
ol 순서가 있다. 1. 2. 3. 이런식
li 리스트 하나하나의 요소를 나타낸다.

리스트 태그 dl dt dd
dl 리스트 전체를 dl로 묶는다.
dt dl 안에 리스트 제목을 dt로 묶어서 쓴다.
dd 리스트 하나하나의 요소를 나타낸다.

표를 나타내는 태그 table
예전에는 웹사이트 전체 레이아웃을 만드는데 썼지만 이젠 사용빈도가 많이 줄었다.
table 로 표가 될 부분 전체를 감싼다.
tr 가로줄 행
td 세로줄 열
border 속성으로 표 둘레의 두께를 결정.

이미지를 출력하는 태그 img
시작, 종료 태그가 하나로 묶여있는 태그
src 속성=이미지가 있는 경로 표현.(필수)
동일한 위치에 있으면 그냥 파일 이름을 적으면 적용되고 다른위치일시 상세경로 표현해야함.
외부에 있는 이미지도 가능.
alt 속성=이미지가 로딩되지 않을시 표기되는 문장. 웹 접근성에 중요. 그림 표현하는 문구
width 속성=이미지의 크기를 표현. 현대에는 잘 쓰이지 않고 css 로 컨트롤

사운드를 출력하는 태그 audio
HTML5에 새롭게 추가되었다. 많이 쓰진 않는다.
src 속성은 음원이 있는 위치.(필수)
controls 화면상에 컨트롤러를 보일것인지(없어도됨)
autoplay 자동재생할것인지(없어도됨. 없으면 자동재생 안됨)
loop 반복재생(없어도됨.없으면 한번만)

a 태그 target 속성의 값
self = 자기 창에 열린다
blank = 새 창에 열린다